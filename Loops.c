// Write a program to take a number from user and print the number 100 times:
 
#include <stdio.h>
int main()
{
    int num;
    //printf("Enter a number: ");
    //scanf("%d", &num);
    for (int i = 0; i < 10; i++)
    {
        printf("%d\n", num);
    }

    return 0;
}